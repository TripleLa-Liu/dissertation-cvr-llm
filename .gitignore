# Raw / derived data (large, not tracked — see README for download + preprocessing steps)
Dataset/
*.csv
*.pkl
*.tar.gz

# Model checkpoints / frozen embedding caches (regenerate locally via src/baselines,
# src/models — see README "Known Gaps"; baseline_results/v1_results/v1_full_results/
# v2_results can each be ~20MB-2GB, not suitable for git)
*.pt
*.pth
baseline_results/
v1_results/
v1_full_results/
v2_results/
migration_package/processed_data/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# LaTeX build artifacts
*.aux
*.log
*.out
*.toc
*.synctex.gz

# OS / editor
.DS_Store
Thumbs.db
